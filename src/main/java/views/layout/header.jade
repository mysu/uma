html
  body
    .navbar.navbar-inverse.navbar-fixed-top
      .container
        .navbar-header
          button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
            span.icon-bar
            span.icon-bar
            span.icon-bar
          a.navbar-brand(href='/') #{i18n.value("header.title")}
        .navbar-collapse.collapse
          ul.nav.navbar-nav
            li.active
              a(href='/') #{i18n.value("header.home")}
          ul.navbar-right.nav.navbar-nav
            if user
              include headerLogged
            else
              li
                a#register(href='/register') #{i18n.value("header.register")}
              li
                a#login(href='/login') #{i18n.value("header.login")}
        // /.navbar-collapse

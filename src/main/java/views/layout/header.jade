html
  body
    .navbar.navbar-inverse.navbar-fixed-top
      .container
        .navbar-header
          button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
            span.icon-bar
            span.icon-bar
            span.icon-bar
          a.navbar-brand(href='/') #{i18n.getValue("header.title")}
        .navbar-collapse.collapse
          ul.nav.navbar-nav
            li.active
              a(href='/') #{i18n.getValue("header.home")}
          ul.navbar-right.nav.navbar-nav
            if userAuth
              li
                a(href='#') #{userAuth.username}
              li
                a(href='/logout') #{i18n.getValue("header.logout")}
            else
              li
                a#login(href='/login') #{i18n.getValue("header.login")}
        // /.navbar-collapse
